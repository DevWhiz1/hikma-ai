import React from 'react';
import UnifiedSmartScheduler from '../../components/scholar/UnifiedSmartScheduler';

const UnifiedSchedulerPage: React.FC = () => {
  return <UnifiedSmartScheduler />;
};

export default UnifiedSchedulerPage;

